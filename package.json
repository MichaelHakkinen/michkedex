{
  "scripts": {
    "dev": "npm run dev --prefix MichKedex",
    "dev:full": "npm run dev:full --prefix MichKedex",
    "build": "npm run build --prefix MichKedex",
    "preview": "npm run preview --prefix MichKedex"
  },
  "dependencies": {
    "vite": "^8.0.11"
  }
}
